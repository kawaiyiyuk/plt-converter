# Generate with: openssl rand -hex 32
PLT_METRICS_TOKEN=replace-with-a-random-production-token
WX_BACKEND_URL=https://api.fengrenjiyi.com
CONVERSION_SERVICE_TOKEN=replace-with-the-same-random-token-as-wx-server

# Optional: override when the default Tencent Cloud PyPI mirror is unavailable.
# PLT_PIP_INDEX_URL=https://pypi.org/simple
